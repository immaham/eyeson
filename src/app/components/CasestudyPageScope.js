const CasestudyPageScope = ({ scope }) => {
  return (
    <section className="casestudy-page-scope">
      <h1>Scope of work</h1>
      <p>{scope}</p>
    </section>
  );
};

export default CasestudyPageScope;
